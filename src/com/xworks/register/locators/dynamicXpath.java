package com.xworks.register.locators;

import org.openqa.selenium.WebDriver;

public class dynamicXpath {
	public static void dynamicXpathLocators(WebDriver driver)
	{
		//input[contains(@name,'value')] when the vlaue of attribute keeps changing use contains
		
		//a[text()='Login'] when the links/texts available and major unique attributes fields are missing for element
		
		//a[contains(text(),'Mobiles')] using both contains and text functions
	
		//a[starts-with(@class,'active')] 
		
		//input[@type='text' or name='email']
		
		//input[@type='text' and name='email']
		
		
		
	}
}
